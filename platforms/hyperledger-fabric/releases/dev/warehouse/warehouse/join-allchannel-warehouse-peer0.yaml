apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: joinchannel-peer0-join-allchannel-warehouse-peer0
  namespace: warehouse-net
  annotations:
    fluxcd.io/automated: "false"
spec:
  interval: 1m
  releaseName: joinchannel-peer0-join-allchannel-warehouse-peer0
  chart:
    spec:
      interval: 1m
      sourceRef:
        kind: GitRepository
        name: flux-local
        namespace: flux-local
      chart: platforms/hyperledger-fabric/charts/fabric-channel-join
  values:
    metadata:
      namespace: warehouse-net
      images:
        fabrictools: ghcr.io/hyperledger/bevel-fabric-tools:2.5.4
        alpineutils: ghcr.io/hyperledger/bevel-alpine:latest

    peer:
      name: peer0
      address: peer0.warehouse-net.svc.cluster.local:7051
      localmspid: warehouseMSP
      loglevel: debug
      tlsstatus: true

    vault:
      role: vault-role
      address: http://45.32.121.191:8200
      authpath: localwarehouse-net-auth
      adminsecretprefix: secretsv2/data/crypto/peerOrganizations/warehouse-net/users/admin
      orderersecretprefix: secretsv2/data/crypto/peerOrganizations/warehouse-net/orderer
      serviceaccountname: vault-auth
      type: hashicorp
      imagesecretname: ""

    channel:
      name: allchannel      
    orderer:
      address: orderer1.supplychain-net.svc.cluster.local:7051
