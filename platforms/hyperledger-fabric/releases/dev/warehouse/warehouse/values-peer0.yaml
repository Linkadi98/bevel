apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: warehouse-peer0
  namespace: warehouse-net
  annotations:
    fluxcd.io/automated: "false"
spec:
  interval: 1m
  releaseName: warehouse-peer0
  chart:
    spec:
      interval: 1m
      sourceRef:
        kind: GitRepository
        name: flux-local
        namespace: flux-local
      chart: platforms/hyperledger-fabric/charts/fabric-peernode    
  values:
    metadata:
      namespace: warehouse-net
      images:
        couchdb:  ghcr.io/hyperledger/bevel-fabric-couchdb:2.5.4
        peer:  ghcr.io/hyperledger/bevel-fabric-peer:2.5.4
        alpineutils: ghcr.io/hyperledger/bevel-alpine:latest

    annotations:  
      service:
        - example1: example2
      pvc:
      deployment:
        
    peer:
      name: peer0
      gossippeeraddress: peer0.warehouse-net:7051
      gossipexternalendpoint: peer0.warehouse-net:7051
      localmspid: warehouseMSP
      loglevel: info
      tlsstatus: true
      builder: hyperledger/fabric-ccenv:2.5.4
      couchdb:
        username: warehouse-user

    storage:
      peer:
        storageclassname: warehouse-minikube-storageclass
        storagesize: 512Mi
      couchdb:
        storageclassname: warehouse-minikube-storageclass
        storagesize: 1Gi

    vault:
      role: vault-role
      address: http://45.32.121.191:8200
      authpath: localwarehouse-net-auth
      secretprefix: secretsv2/data/crypto/peerOrganizations/warehouse-net/peers/peer0.warehouse-net
      serviceaccountname: vault-auth
      type: hashicorp
      imagesecretname: ""
      secretcouchdbpass: secretsv2/data/credentials/warehouse-net/couchdb/warehouse?user

    service:
      servicetype: ClusterIP
      ports:
        grpc:
          clusteripport: 7051
        events:
          clusteripport: 7053
        couchdb:
          clusteripport: 5984
        metrics: 
          enabled: False
          clusteripport: 9443     
    proxy:
      provider: "none"
      external_url_suffix: org5proxy

    config:
      pod:
        resources:
          limits:
            memory: 512M
            cpu: 1
          requests:
            memory: 512M
            cpu: 0.25
